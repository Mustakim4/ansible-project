# ansible-project
ansible test project

## if you want to use this repo or ansible structure just clone it and go to roles folder create role ansible-galaxy init role-name and add it to playbooks/main.yml file
